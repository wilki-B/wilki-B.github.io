{
  "name": "tu-app",
  "version": "1.0.0",
  "engines": {
    "node": "18.x",  // Versión específica
    "npm": "9.x"     // Evita que Render use Bun
  },
  "scripts": {
    "start": "node servidor.js",
    "build": "npm install"
  },
  "dependencies": {
    "express": "^5.1.0",
    "mongodb": "^6.6.0"
  }
}
